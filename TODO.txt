fd todo:
- Write a method to check if a move is legal (maybe making use of the above method).  Before inserting a fresh node into the tree this checkMove() should be consulted first.
- Figure out how to write UI unit tests (to test stuff like the UI tree operation programmatically. (!BIG WIN!)
