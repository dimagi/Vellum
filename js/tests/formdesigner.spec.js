/* global describe, it */
if (typeof define !== 'function') { var define = require('amdefine')(module); }
define([
    'formdesigner'
], function(formdesigner) {
    "use strict";

    describe("A test section", function() {
        it("A property of formdesigner", function() {
            // No op
        });
    });
});
